<import src="../movie/movie-template.wxml" />
<template name="movieGridTemplate">
    <view>
        <block wx:for="{{movies}}" wx:for-item="movie" wx:key="movie-grid">
            <template is="movieTemplate" data="{{...movie}}" />
        </block>
    </view>
</template>